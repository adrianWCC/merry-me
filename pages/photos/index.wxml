<view class="container">
	<view class="swiper">
		<swiper autoplay interval="5000" vertical circular>
			<swiper-item wx:for="{{slideList}}" wx:key="index">
				<image src="{{item}}" class="slide-image" mode="aspectFill" />
			</swiper-item>
		</swiper>
	</view>
</view>